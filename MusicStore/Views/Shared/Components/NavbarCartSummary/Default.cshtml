@using System
@model int

<li class="nav-item">
    <a class="nav-link text-dark" asp-controller="ShoppingCart" asp-action="Index" title="Shopping cart"><i class="fas fa-shopping-cart"></i>
        @if (Model > 0)
        {
            <span class="badge bg-secondary">@Model.ToString()</span>

        }
    </a>
</li>